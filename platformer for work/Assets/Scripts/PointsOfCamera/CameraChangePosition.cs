using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraChangePosition : MonoBehaviour
{

    /*List<PointsOfCamera> allPoints = new List<PointsOfCamera>();
    PointsOfCamera[] pointsMass;
    private void Awake()
    {
        int sumOfPoints = GameObject.FindGameObjectsWithTag("CameraPoint").Length;
        pointsMass = new PointsOfCamera[sumOfPoints];
        for (int i = 0; i < pointsMass.Length; i++)
        {
            PointsOfCamera point = gameObject.transform.GetChild(i).GetComponent<PointsOfCamera>();
            pointsMass[i] = point;
        }
    }*/
}
